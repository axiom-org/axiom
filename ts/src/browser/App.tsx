import * as React from "react";
import AxiomAPI from "./AxiomAPI";

export default function App() {
  let axiom = new AxiomAPI({ network: "alpha", verbose: true });
  return <div>TODO: put something here</div>;
}
